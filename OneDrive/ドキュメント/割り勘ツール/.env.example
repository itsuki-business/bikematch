# Environment variables for Vite (client-exposed must start with VITE_)
# ------------------------------------------------------------------
# 任意: CloudFront/S3/Amplify でサブパス配下にホストする場合のみ設定
# 例: /warikan
# VITE_BASE_PATH=

# メモ:
# - ローカル: .env を作成して上記を設定
# - AWS Amplify: 環境変数に同名キーを設定（VITE_* はビルド時にクライアントへ公開）
